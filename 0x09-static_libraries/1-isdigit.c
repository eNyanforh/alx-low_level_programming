#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @ny: int to be checked
 * Return: 1 if ny is a digit, 0 otherwise
 */
int _isdigit(int ny)
{
	return (ny >= '0' && ny <= '9');
}
