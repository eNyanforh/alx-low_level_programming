#include "main.h"

/**
 * _islower - checks for lowercase character
 * @ny: the character to check
 * Return: 1 if ny is lowercase, 0 otherwise
 */
int _islower(int ny)
{
	return (ny >= 'a' && ny <= 'z');
}
